# Cloudflare Pages configuration for the-brain.dev
# This project uses GitHub integration — Cloudflare Pages auto-deploys from out/
# Build settings in Cloudflare dashboard:
#   Build command:   bun run build
#   Output directory: out
#   Deploy command:   (leave empty — Pages handles deployment)
name = "the-brain-docs"
compatibility_date = "2026-05-06"
